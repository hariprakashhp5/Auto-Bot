<%= form_for(@roro) do |f| %>
  <% if @roro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roro.errors.count, "error") %> prohibited this roro from being saved:</h2>

      <ul>
      <% @roro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
